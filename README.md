# split-it-app-backend
A Splitwise-like mobile app built with React Native (Expo, TypeScript) and a Node.js (TypeScript) backend. Uses Firebase Firestore, email-based OTP authentication with JWT, and email notifications. Clean, scalable architecture with modern UI, backend-hosted, mobile in test mode only.
